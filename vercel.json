{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/api/cron/**/*.js": {
      "maxDuration": 30
    },
    "app/api/**/*.js": {
      "maxDuration": 10
    }
  }
}
